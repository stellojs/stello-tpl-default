<!DOCTYPE HTML>
<html>
  <head>
    <title>{{title}}</title>
  </head>
  <body>
    <h1>{{title}}</h1>
    <p>{{description}}</p>

    <ul>
      {{#each posts}}
      <li>{{title}}</li>
      {{/each}}
    </ul>
  </body>
</html>
